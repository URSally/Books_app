<h1>編集画面</h1>

<%= form_with model: @book do |f| %>

  <h4>タイトル</h4>
  <%= f.text_field :title %>
  
  <h4>本文</h4>
  <%= f.text_area :body %>
  
  <%= f.submit 'Update Book' %>
  
<% end %>

</form>
<p class="edit">
<a><%=link_to "Show", book_path(@book.id) %></a>
|
<a href="/books">Back</a>
</p>